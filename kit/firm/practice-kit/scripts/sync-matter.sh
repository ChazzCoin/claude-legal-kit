#!/usr/bin/env bash
#
# sync-matter.sh — pull updated shared-library/ content into an existing matter.
#
# STATUS: NOT YET IMPLEMENTED — skeleton stub. See scripts/README.md.
#
# Planned behavior:
#   - Pull updated shared-library/ content into an existing matter.
#   - Surface any locally-overridden files; never overwrite silently.
#
# Windows counterpart: sync-matter.ps1. Claude picks the right flavor from
# .claude/platform.json — see conduct/running-scripts.md.

set -eu
echo "sync-matter.sh is not yet implemented — see firm/practice-kit/scripts/README.md" >&2
exit 1
